import styled from '@emotion/styled';

export const StoreDesc = styled.div`
  color: rgba(23, 30, 63, 0.5);

  font-size: 15px;
  font-weight: 500;
  line-height: 29px;
  text-align: right;
  margin-top: 2rem;
`;
