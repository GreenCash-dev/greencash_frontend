import styled from '@emotion/styled';

export const MainListContainer = styled.div`
  margin-left: 11rem;
  margin-top: 13rem;
`;

export const MainList = styled.div`
  color: rgba(23, 30, 63, 1);
  font-size: 4rem;
  font-weight: Bold;
  margin-bottom: 4.5rem;
`;

export const MainSideList = styled.div`
  color: rgba(23, 30, 63, 0.5);
  font-size: 17px;
  font-weight: 500;
  line-height: 45px;
`;
